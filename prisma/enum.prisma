
enum UserRole {
  SUPER_ADMIN
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  BANNED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum TravelType {
  SOLO
  FRIENDS
  FAMILY
  GROUP
}

enum SubscriptionPlan {
  
  MONTHLY
  YEARLY
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
}

enum PlanVisibility {
  PUBLIC
  PRIVATE
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum ReactionType {
  LIKE
  LOVE
  WOW
  SAD
  ANGRY
}